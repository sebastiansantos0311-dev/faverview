[project]
name = "faverview"
version = "3.0.0.dev2"
description = "Compara el arte del cliente con tu diseño en PDF y marca las diferencias."
readme = "README.md"
license = "AGPL-3.0-or-later"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "python-multipart>=0.0.9",
    "pymupdf>=1.24",
    "pillow>=10.3",
    "opencv-python-headless>=4.9",
    "scikit-image>=0.23",
    "numpy>=1.26",
    "pytesseract>=0.3.10",
    "rapidfuzz>=3.9",
    "pyspellchecker>=0.8.1",
    "imagehash>=4.3",
    "scipy>=1.11",
    "spylls>=0.1.7",
    "pikepdf>=9.0",
    "vtracer>=0.6",
    "potracer>=0.0.4",
    "ezdxf>=1.3",
    "zxing-cpp>=2.2",
    "treepoem>=3.24",
    "segno>=1.6",
    "watchdog>=4.0",
    "lxml>=5.0",
]

[project.scripts]
faverview = "app.launcher:main"
faverview-aprender = "app.learning.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "httpx>=0.27",
    "pytest-cov>=5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
